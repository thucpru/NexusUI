{
  "name": "@nexusui/plugin",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node esbuild.config.js --watch",
    "build": "node esbuild.config.js",
    "lint": "eslint src --ext .ts,.tsx",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@nexusui/shared": "workspace:*",
    "preact": "^10.20.0"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.92.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.21.5",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.0"
  }
}
